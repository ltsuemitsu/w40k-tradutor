# Core translation requirements (already needed)
openai>=1.0.0
tqdm>=4.66

# GUI - PySide6 (recommended for rich tables, theming, and desktop feel)
# Run: pip install -r requirements-gui.txt
PySide6>=6.6

# Secure API-key storage via the OS keychain (Windows Credential Manager etc.)
keyring>=25.0
